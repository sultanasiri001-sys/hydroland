import { readFile } from 'node:fs/promises'; import path from 'node:path'; import { fileURLToPath } from 'node:url';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'); const files=await Promise.all(['src/integrations/integration.service.ts','src/agents/agent.service.ts','src/integrations/integration.controller.ts'].map(file=>readFile(path.join(root,file),'utf8')));const source=files.join('\n');for(const token of ['timingSafeEqual','HYDROLAND_WEBHOOK_','OVERRIDE_SAFETY_DECISION','OVERRIDE_REGULATORY_BLOCK','REQUEST_HUMAN_APPROVAL','ar-SA','agent.plan.created'])if(!source.includes(token))throw new Error(`Missing Phase 10 control: ${token}`);console.log('Validated integration signature, replay window, agent boundaries, Arabic Saudi support, and audit events.');
