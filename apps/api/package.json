{
  "name": "@hydroland/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "nest start --watch",
    "build": "nest build",
    "start": "node dist/main.js",
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc --noEmit",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:deploy": "prisma migrate deploy",
    "validate:hr": "node scripts/hr-validation.mjs",
    "validate:operations": "rm -rf .tmp-operations-validation && tsc src/operations/operations-policy.ts src/operations/operations-policy.spec.ts --outDir .tmp-operations-validation --module commonjs --target es2020 --esModuleInterop --skipLibCheck && node .tmp-operations-validation/operations-policy.spec.js && rm -rf .tmp-operations-validation",
    "validate:inventory": "rm -rf .tmp-inventory-validation && tsc src/inventory/inventory-policy.ts src/inventory/inventory-policy.spec.ts --outDir .tmp-inventory-validation --module commonjs --target es2020 --esModuleInterop --skipLibCheck && node .tmp-inventory-validation/inventory-policy.spec.js && rm -rf .tmp-inventory-validation",
    "validate:phase10": "node scripts/phase10-validation.mjs",
    "validate:phase12": "node scripts/phase12-validation.mjs"
  },
  "dependencies": {
    "@nestjs/common": "^11.2.3",
    "@nestjs/core": "^11.2.3",
    "@nestjs/platform-express": "^11.2.3",
    "@prisma/client": "^6.19.3",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.0",
    "@types/node": "^22.10.0",
    "prisma": "^6.19.3",
    "typescript": "^5.7.0"
  }
}
