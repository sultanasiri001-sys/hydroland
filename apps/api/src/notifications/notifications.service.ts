import { Injectable, NotFoundException } from '@nestjs/common'; import { DatabaseService } from '../database/database.service'; @Injectable() export class NotificationsService {constructor(private readonly db:DatabaseService){} list(accountId:string){return this.db.notification.findMany({where:{accountId},orderBy:{createdAt:'desc'},take:100})} async read(accountId:string,id:string){const r=await this.db.notification.updateMany({where:{id,accountId},data:{status:'READ'}});if(!r.count)throw new NotFoundException('Notification not found.');return{id,status:'READ'}}}