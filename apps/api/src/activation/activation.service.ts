import { BadRequestException, ConflictException, Injectable, NotFoundException } from '@nestjs/common';
import { Prisma } from '@prisma/client';
import { DatabaseService } from '../database/database.service';
@Injectable() export class ActivationService {
 constructor(private readonly db:DatabaseService){}
 async request(accountId:string,role:string){const roleAssignment=await this.db.roleAssignment.upsert({where:{accountId_role:{accountId,role:role as never}},create:{accountId,role:role as never,status:'PENDING_REVIEW'},update:{}});const existing=await this.db.activationRequest.findFirst({where:{applicantId:accountId,roleAssignmentId:roleAssignment.id,status:{in:['DRAFT','SUBMITTED','UNDER_REVIEW','MORE_INFORMATION_REQUIRED','RESUBMITTED']}}});if(existing)throw new ConflictException('An active request already exists.');return this.db.activationRequest.create({data:{applicantId:accountId,roleAssignmentId:roleAssignment.id,status:'SUBMITTED',submittedAt:new Date()}})}
 async mine(accountId:string){return this.db.activationRequest.findMany({where:{applicantId:accountId},include:{roleAssignment:true,decisions:true},orderBy:{createdAt:'desc'}})}
 async decide(reviewerId:string,requestId:string,outcome:'APPROVED'|'REJECTED',reason?:string){const request=await this.db.activationRequest.findUnique({where:{id:requestId}});if(!request||request.applicantId===reviewerId)throw new NotFoundException('Request not reviewable.');if(request.status!=='SUBMITTED'&&request.status!=='UNDER_REVIEW')throw new BadRequestException('Invalid request state.');return this.db.$transaction(async (tx: Prisma.TransactionClient)=>{await tx.reviewDecision.create({data:{activationRequestId:requestId,reviewerId,outcome,reason}});await tx.activationRequest.update({where:{id:requestId},data:{status:outcome,decidedAt:new Date()}});await tx.roleAssignment.update({where:{id:request.roleAssignmentId},data:{status:outcome==='APPROVED'?'ACTIVE':'REJECTED',activeAt:outcome==='APPROVED'?new Date():undefined}});return {id:requestId,status:outcome}})}
}