import {Controller,Get,UseGuards} from '@nestjs/common'; import {AccessTokenGuard} from '../auth/access-token.guard'; import {AdminGuard} from './admin.guard'; import {AdminService} from './admin.service'; @UseGuards(AccessTokenGuard,AdminGuard) @Controller('admin') export class AdminController {constructor(private readonly s:AdminService){} @Get('overview') overview(){return this.s.overview()} @Get('review-queue') queue(){return this.s.queue()}}