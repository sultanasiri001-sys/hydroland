import { Injectable } from '@nestjs/common'; import { DatabaseService } from '../database/database.service'; @Injectable() export class AdminService {constructor(private readonly db:DatabaseService){} async overview(){const [accounts,pending,openTrips,bookings]=await Promise.all([this.db.account.count(),this.db.activationRequest.count({where:{status:{in:['SUBMITTED','UNDER_REVIEW','RESUBMITTED']}}}),this.db.trip.count({where:{status:'OPEN'}}),this.db.booking.count({where:{status:{in:['PENDING','CONFIRMED']}}})]);return{accounts,pendingReviews:pending,openTrips,activeBookings:bookings}} queue(){return this.db.activationRequest.findMany({where:{status:{in:['SUBMITTED','UNDER_REVIEW','RESUBMITTED']}},include:{applicant:{select:{id:true,email:true}},roleAssignment:true},orderBy:{createdAt:'asc'}})}}