import { Body, Controller, Param, Post, UseGuards } from '@nestjs/common'; import { AccessTokenGuard } from '../auth/access-token.guard'; import { SafetyService } from './safety.service'; @UseGuards(AccessTokenGuard) @Controller('trips/:tripId/safety') export class SafetyController {constructor(private readonly s:SafetyService){} @Post() assess(@Param('tripId') id:string,@Body() b:{items:Record<string,boolean>;notes?:string}){return this.s.assess(id,b)}}