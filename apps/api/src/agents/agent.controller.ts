import { Body, Controller, Get, Post, Req, UseGuards } from '@nestjs/common'; import { AccessTokenGuard } from '../auth/access-token.guard'; import { AgentAction,AgentKey,SupportedLanguage } from './agent.types'; import { AgentService } from './agent.service';
@UseGuards(AccessTokenGuard) @Controller('agents') export class AgentController {constructor(private readonly service:AgentService){} @Get('catalog')catalog(){return this.service.list()} @Post('plans')plan(@Req()request:{auth:{accountId:string}},@Body()body:{agent:AgentKey;action:AgentAction;language:SupportedLanguage;prompt:string}){return this.service.plan(request.auth.accountId,body)}}
