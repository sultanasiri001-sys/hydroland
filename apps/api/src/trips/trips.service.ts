import { BadRequestException, ConflictException, Injectable, NotFoundException } from '@nestjs/common'; import { Prisma } from '@prisma/client'; import { DatabaseService } from '../database/database.service';
@Injectable() export class TripsService {constructor(private readonly db:DatabaseService){} list(){return this.db.trip.findMany({where:{status:'OPEN'},orderBy:{startsAt:'asc'}})} async book(accountId:string,tripId:string,seats:number){if(!Number.isInteger(seats)||seats<1)throw new BadRequestException('Invalid seats.');return this.db.$transaction(async(tx:Prisma.TransactionClient)=>{const trip=await tx.trip.findUnique({where:{id:tripId}});if(!trip||trip.status!=='OPEN')throw new NotFoundException('Trip unavailable.');const used=await tx.booking.aggregate({where:{tripId,status:{in:['PENDING','CONFIRMED']}},_sum:{seats:true}});if((used._sum.seats??0)+seats>trip.capacity)throw new ConflictException('Trip capacity reached.');return tx.booking.create({data:{tripId,accountId,seats,status:'PENDING'}})})} mine(accountId:string){return this.db.booking.findMany({where:{accountId},include:{trip:true},orderBy:{createdAt:'desc'}})}}