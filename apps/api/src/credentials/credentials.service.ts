import { BadRequestException, Injectable, NotFoundException } from '@nestjs/common';
import { DatabaseService } from '../database/database.service';
@Injectable()
export class CredentialsService {
  constructor(private readonly db: DatabaseService) {}
  async list(accountId:string){const a=await this.db.account.findUniqueOrThrow({where:{id:accountId},select:{personId:true}});return this.db.credential.findMany({where:{personId:a.personId},include:{documents:true},orderBy:{createdAt:'desc'}})}
  async create(accountId:string,input:{issuer:string;title:string;credentialNumber?:string;issuedAt?:string;expiresAt?:string}){if(!input.issuer?.trim()||!input.title?.trim())throw new BadRequestException('Issuer and title are required.');const a=await this.db.account.findUniqueOrThrow({where:{id:accountId},select:{personId:true}});return this.db.credential.create({data:{personId:a.personId,issuer:input.issuer.trim(),title:input.title.trim(),credentialNumber:input.credentialNumber,issuedAt:input.issuedAt?new Date(input.issuedAt):undefined,expiresAt:input.expiresAt?new Date(input.expiresAt):undefined}})}
  async attachDocument(accountId:string,credentialId:string,input:{storageKey:string;originalName:string;mimeType:string;byteSize:number;sha256:string}){if(!['application/pdf','image/jpeg','image/png'].includes(input.mimeType)||input.byteSize<1||input.byteSize>10_000_000)throw new BadRequestException('Unsupported document.');const a=await this.db.account.findUniqueOrThrow({where:{id:accountId},select:{personId:true}});const c=await this.db.credential.findFirst({where:{id:credentialId,personId:a.personId}});if(!c)throw new NotFoundException('Credential not found.');return this.db.document.create({data:{credentialId,ownerId:a.personId,...input,status:'UPLOADED'}})}
}