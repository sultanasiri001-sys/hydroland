import { BadRequestException, Injectable, NotFoundException } from '@nestjs/common'; import { DatabaseService } from '../database/database.service'; @Injectable() export class PaymentsService {constructor(private readonly db:DatabaseService){} async create(accountId:string,input:{bookingId:string;amountMinor:number;idempotencyKey:string}){if(!Number.isInteger(input.amountMinor)||input.amountMinor<1||!input.idempotencyKey)throw new BadRequestException('Invalid payment.');const booking=await this.db.booking.findFirst({where:{id:input.bookingId,accountId}});if(!booking)throw new NotFoundException('Booking not found.');return this.db.payment.upsert({where:{idempotencyKey:input.idempotencyKey},create:{...input,accountId},update:{}})} mine(accountId:string){return this.db.payment.findMany({where:{accountId},include:{invoice:true},orderBy:{createdAt:'desc'}})}}