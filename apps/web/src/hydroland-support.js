(()=>{const community=document.getElementById('community');if(!community||document.querySelector('.hl-support'))return;const Auth=window.HydrolandAuth;const wrap=document.createElement('section');wrap.className='hl-support';wrap.innerHTML=`<header class="hl-support-head"><div><small>SUPPORT CENTER · HYDROLAND</small><h3>خدمة العملاء والدعم</h3></div><span data-support-state>يتطلب تسجيل الدخول</span></header><div class="hl-support-grid"><section class="hl-support-panel"><h4>إنشاء تذكرة</h4><form class="hl-support-input" data-support-form><select name="type" aria-label="نوع الطلب"><option value="SUPPORT">دعم</option><option value="QUESTION">استفسار</option><option value="COMPLAINT">شكوى</option><option value="BOOKING_ISSUE">مشكلة حجز</option><option value="PAYMENT_ISSUE">مشكلة دفع</option><option value="SAFETY_CONCERN">ملاحظة سلامة</option></select><input name="subject" maxlength="160" aria-label="عنوان التذكرة" placeholder="عنوان التذكرة" required><textarea name="description" maxlength="3000" aria-label="تفاصيل التذكرة" placeholder="اكتب التفاصيل" required></textarea><button type="submit">إرسال التذكرة</button></form></section><section class="hl-support-panel"><h4>تذاكري</h4><div class="hl-ticket-list" data-support-list><div class="hl-ticket"><span><strong>سجّل الدخول لعرض التذاكر</strong></span><b class="hl-status closed">—</b></div></div></section><section class="hl-support-panel"><h4>حالة الخدمة</h4><p data-support-message>تُحفظ التذاكر في حسابك ويمكن متابعتها من هذه الصفحة.</p><button type="button" data-support-refresh>تحديث التذاكر</button></section></div><div class="hl-support-metrics"><article><small>تذاكر فعلية</small><b data-support-count>—</b></article><article><small>حالة الربط</small><b data-support-link>API</b></article></div>`;community.insertAdjacentElement('afterend',wrap);
const state=wrap.querySelector('[data-support-state]'),list=wrap.querySelector('[data-support-list]'),count=wrap.querySelector('[data-support-count]'),message=wrap.querySelector('[data-support-message]'),form=wrap.querySelector('[data-support-form]');
const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const label={OPEN:'مفتوحة',ASSIGNED:'مسندة',WAITING_CUSTOMER:'بانتظارك',RESOLVED:'محلولة',CLOSED:'مغلقة'};
async function load(){if(!Auth?.authorizedFetch){state.textContent='يتطلب تسجيل الدخول';return;}try{const r=await Auth.authorizedFetch('/customer-service/cases/mine');if(!r.ok)throw new Error('LOAD_FAILED');const rows=await r.json();state.textContent='متصل بالنظام';count.textContent=String(rows.length);list.innerHTML=rows.length?rows.map(x=>`<div class="hl-ticket"><span><strong>${esc(x.subject)}</strong><small>${esc(x.type)} · ${new Date(x.createdAt).toLocaleDateString('ar-SA')}</small></span><b class="hl-status">${esc(label[x.status]||x.status||'—')}</b></div>`).join(''):'<div class="hl-ticket"><span><strong>لا توجد تذاكر</strong><small>يمكنك إنشاء أول تذكرة من النموذج.</small></span><b class="hl-status closed">—</b></div>';}catch{state.textContent='تعذر تحميل الخدمة';count.textContent='—';}}
form.addEventListener('submit',async e=>{e.preventDefault();if(!Auth?.authorizedFetch){message.textContent='سجّل الدخول أولًا.';return;}const data=new FormData(form),body={type:data.get('type'),subject:String(data.get('subject')||'').trim(),description:String(data.get('description')||'').trim()};if(!body.subject||!body.description)return;const button=form.querySelector('button');button.disabled=true;try{const r=await Auth.authorizedFetch('/customer-service/cases',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});if(!r.ok)throw new Error('CREATE_FAILED');form.reset();message.textContent='تم تسجيل التذكرة في النظام.';await load();}catch{message.textContent='تعذر تسجيل التذكرة. تحقق من تسجيل الدخول وحاول مرة أخرى.';}finally{button.disabled=false;}});
wrap.querySelector('[data-support-refresh]').addEventListener('click',load);load();})();