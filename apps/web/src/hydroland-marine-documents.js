(()=>{
  if(document.getElementById('hl-marine-documents'))return;
  const host=document.getElementById('role-console');if(!host)return;
  const panel=document.createElement('section');panel.id='hl-marine-documents';panel.hidden=true;panel.innerHTML=`<style>
  #hl-marine-documents{margin:1rem 0;padding:1rem;border:1px solid rgba(120,191,224,.18);border-radius:20px;background:rgba(3,25,39,.92);color:#eaf5fa}#hl-marine-documents[hidden]{display:none}.hl-marine-head,.hl-marine-row,.hl-marine-actions{display:flex;gap:.65rem;align-items:center;justify-content:space-between;flex-wrap:wrap}.hl-marine-grid{display:grid;grid-template-columns:minmax(260px,.75fr) minmax(320px,1.25fr);gap:1rem}.hl-marine-card,.hl-marine-asset{border:1px solid rgba(120,191,224,.14);border-radius:16px;padding:.85rem;background:rgba(255,255,255,.025)}.hl-marine-form{display:grid;gap:.55rem}.hl-marine-form label{display:grid;gap:.25rem;font-weight:700}.hl-marine-form input,.hl-marine-form select{width:100%;box-sizing:border-box;border:1px solid rgba(120,191,224,.2);border-radius:10px;background:#0b293c;color:#fff;padding:.6rem}.hl-marine-actions{justify-content:flex-start;margin-top:.6rem}#hl-marine-documents button{border:1px solid rgba(120,191,224,.22);border-radius:10px;background:#12364b;color:#fff;padding:.55rem .75rem;font-weight:800;cursor:pointer}#hl-marine-documents button.primary{border:0;background:linear-gradient(135deg,#f4d18c,#e5b45f);color:#102131}.hl-marine-doc{display:flex;gap:.5rem;justify-content:space-between;align-items:center;padding:.45rem 0;border-bottom:1px solid rgba(120,191,224,.1)}.hl-marine-note{color:#9cb7c7}.hl-marine-state{font-size:.78rem;padding:.2rem .45rem;border-radius:999px;background:rgba(229,180,95,.14);color:#f3d496}@media(max-width:760px){.hl-marine-grid{grid-template-columns:1fr}}</style>
  <header class="hl-marine-head"><div><small>MARINE DOCUMENT CONTROL · الوساطة البحرية</small><h3>الوثائق والتراخيص البحرية</h3><p class="hl-marine-note">إدارة بيانات التسجيل ورخصة الملاحة وشهادة السلامة. رفع ملف المستند نفسه سيُربط مع التخزين الخارجي في مرحلة التكاملات النهائية.</p></div><button type="button" data-marine-refresh>تحديث</button></header><div data-marine-content></div>`;
  host.insertAdjacentElement('afterend',panel);
  const content=panel.querySelector('[data-marine-content]');
  const state={role:'diver',memberships:[],assets:[],pending:[]};
  const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const request=async(path,options={})=>{const response=await window.HydrolandAuth.authorizedFetch(path,options);const body=await response.json().catch(()=>null);if(!response.ok)throw new Error(body?.message||`HTTP ${response.status}`);return body};
  const docName=t=>({REGISTRATION:'تسجيل القارب',NAVIGATION_LICENSE:'رخصة الملاحة',SAFETY_CERTIFICATE:'شهادة السلامة'}[t]||t);
  const fmtDate=v=>v?new Date(v).toLocaleDateString('ar-SA'):'بدون انتهاء';
  function renderBoat(){const orgOptions=state.memberships.map(m=>`<option value="${esc(m.organization.id)}">${esc(m.organization.displayName)}</option>`).join('');content.innerHTML=`<div class="hl-marine-grid"><form class="hl-marine-card hl-marine-form" data-marine-asset-form><h4>إضافة قارب / أصل بحري</h4><label>الجهة<select name="organizationId" required>${orgOptions}</select></label><label>الاسم<input name="name" required maxlength="120"></label><label>النوع<select name="assetType"><option>DIVE_BOAT</option><option>YACHT</option><option>RIB</option><option>SUPPORT_VESSEL</option><option>OTHER</option></select></label><label>رقم التسجيل<input name="registrationNumber" maxlength="80"></label><label>سعة الركاب<input name="passengerCapacity" type="number" min="1"></label><button class="primary" type="submit" ${orgOptions?'':'disabled'}>حفظ الأصل البحري</button></form><section class="hl-marine-card"><h4>الأصول والوثائق</h4><div data-marine-assets>${state.assets.length?state.assets.map(asset=>`<article class="hl-marine-asset" data-marine-asset="${esc(asset.id)}"><div class="hl-marine-row"><div><strong>${esc(asset.name)}</strong><small> · ${esc(asset.assetType)} · ${esc(asset.registrationNumber||'بدون رقم')}</small></div><span class="hl-marine-state">${esc(asset.status)}</span></div><div>${(asset.documents||[]).length?(asset.documents||[]).map(d=>`<div class="hl-marine-doc"><span>${esc(docName(d.documentType))} · ${esc(d.referenceNumber||'بدون مرجع')} · ${esc(fmtDate(d.expiresAt))}</span><b class="hl-marine-state">${esc(d.status)}</b></div>`).join(''):'<p class="hl-marine-note">لا توجد وثائق مسجلة.</p>'}</div><form class="hl-marine-form" data-marine-doc-form style="margin-top:.7rem"><label>نوع الوثيقة<select name="documentType"><option value="REGISTRATION">تسجيل القارب</option><option value="NAVIGATION_LICENSE">رخصة الملاحة</option><option value="SAFETY_CERTIFICATE">شهادة السلامة</option></select></label><label>الرقم المرجعي<input name="referenceNumber" maxlength="120"></label><label>تاريخ الانتهاء<input name="expiresAt" type="date"></label><button type="submit">إرسال للمراجعة</button></form></article>`).join(''):'<p class="hl-marine-note">لا توجد أصول بحرية مرتبطة بعضويتك بعد.</p>'}</div></section></div>`}
  function renderAdmin(){content.innerHTML=`<section class="hl-marine-card"><h4>مراجعة وثائق الأصول البحرية</h4>${state.pending.length?state.pending.map(d=>`<article class="hl-marine-asset" data-marine-doc="${esc(d.id)}"><div class="hl-marine-row"><div><strong>${esc(d.marineAsset?.name||'أصل بحري')}</strong><small> · ${esc(docName(d.documentType))} · ${esc(d.referenceNumber||'بدون مرجع')} · ${esc(fmtDate(d.expiresAt))}</small></div><span class="hl-marine-state">PENDING</span></div><div class="hl-marine-actions"><button type="button" data-marine-decision="VERIFIED">اعتماد</button><button type="button" data-marine-decision="REJECTED">رفض</button></div></article>`).join(''):'<p class="hl-marine-note">لا توجد وثائق بحرية بانتظار المراجعة.</p>'}</section>`}
  async function loadBoat(){const [memberships,assets]=await Promise.all([request('/organizations/mine'),request('/marine-operations/assets/mine')]);state.memberships=(Array.isArray(memberships)?memberships:[]).filter(x=>x.status==='ACTIVE');state.assets=Array.isArray(assets)?assets:[];renderBoat()}
  async function loadAdmin(){const pending=await request('/marine-operations/admin/documents/pending');state.pending=Array.isArray(pending)?pending:[];renderAdmin()}
  async function refresh(){if(!window.HydrolandAuth?.isAuthenticated?.()){panel.hidden=true;return}try{if(state.role==='boat')await loadBoat();else if(state.role==='admin')await loadAdmin()}catch(error){content.innerHTML=`<p class="hl-marine-note">${esc(error instanceof Error?error.message:'تعذر تحميل البيانات البحرية')}</p>`}}
  function open(role){state.role=role;panel.hidden=false;refresh();panel.scrollIntoView({behavior:'smooth',block:'start'})}
  panel.querySelector('[data-marine-refresh]')?.addEventListener('click',refresh);
  content.addEventListener('submit',async event=>{event.preventDefault();const form=event.target;if(form.matches('[data-marine-asset-form]')){const data=Object.fromEntries(new FormData(form).entries());if(data.passengerCapacity)data.passengerCapacity=Number(data.passengerCapacity);else delete data.passengerCapacity;try{await request('/marine-operations/assets',{method:'POST',body:JSON.stringify(data)});await loadBoat()}catch(error){alert(error instanceof Error?error.message:'تعذر حفظ الأصل البحري')}return}if(form.matches('[data-marine-doc-form]')){const asset=form.closest('[data-marine-asset]');const data=Object.fromEntries(new FormData(form).entries());if(!data.expiresAt)delete data.expiresAt;try{await request(`/marine-operations/assets/${encodeURIComponent(asset.dataset.marineAsset)}/documents`,{method:'POST',body:JSON.stringify(data)});await loadBoat()}catch(error){alert(error instanceof Error?error.message:'تعذر حفظ الوثيقة')}}});
  content.addEventListener('click',async event=>{const button=event.target.closest('[data-marine-decision]');if(!button)return;const card=button.closest('[data-marine-doc]');button.disabled=true;try{await request(`/marine-operations/admin/documents/${encodeURIComponent(card.dataset.marineDoc)}/decision`,{method:'POST',body:JSON.stringify({outcome:button.dataset.marineDecision})});await loadAdmin()}catch(error){alert(error instanceof Error?error.message:'تعذر تنفيذ القرار')}finally{button.disabled=false}});
  const connect=root=>root.querySelectorAll?.('button').forEach(button=>{if((button.textContent||'').trim()!=='الوثائق والتراخيص'||button.dataset.hlMarineConnected)return;button.dataset.hlMarineConnected='1';button.disabled=false;button.removeAttribute('aria-disabled');button.title='';button.addEventListener('click',()=>open('boat'))});
  const observer=new MutationObserver(records=>records.forEach(record=>record.addedNodes.forEach(node=>{if(node.nodeType===1)connect(node)})));observer.observe(document.body,{childList:true,subtree:true});
  document.addEventListener('hydroland:role-changed',event=>{const role=event.detail?.role;if(role==='boat'||role==='admin')open(role);else panel.hidden=true});
  document.addEventListener('hydroland:auth-changed',()=>{if(!window.HydrolandAuth?.isAuthenticated?.())panel.hidden=true});
  const initial=window.HydrolandPortalAccess?.getCurrentRole?.();if(initial==='boat'||initial==='admin')open(initial);
  window.HydrolandMarineDocuments={open,refresh};
})();
