(()=>{
  const auth=()=>window.HydrolandAuth;
  const toast=message=>{const t=document.getElementById('toast');if(!t)return;t.textContent=message;t.classList.add('visible');setTimeout(()=>t.classList.remove('visible'),2400)};
  const request=async(path,options={})=>{const client=auth()?.authorizedFetch;if(!client)throw new Error('AUTH_REQUIRED');const response=await client(path,options);const body=await response.json().catch(()=>null);if(!response.ok){if(response.status===401)throw new Error('انتهت الجلسة، سجّل الدخول من جديد');throw new Error(body?.message||`تعذر تنفيذ الطلب (${response.status})`)}return body;};
  const esc=value=>String(value??'').replace(/[&<>'"]/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[char]));
  const sar=halala=>new Intl.NumberFormat('ar-SA',{style:'currency',currency:'SAR'}).format(Number(halala||0)/100);
  const ensureStyle=()=>{if(document.getElementById('hl-account-center-style'))return;const style=document.createElement('style');style.id='hl-account-center-style';style.textContent=`.hl-account-dialog{width:min(760px,calc(100vw - 24px));max-height:min(82vh,760px);border:1px solid rgba(120,191,224,.2);border-radius:24px;background:#071f31;color:#edf7fb;padding:0;box-shadow:0 28px 90px rgba(0,0,0,.45)}.hl-account-dialog::backdrop{background:rgba(1,10,18,.7);backdrop-filter:blur(5px)}.hl-account-head{display:flex;align-items:center;justify-content:space-between;padding:1rem 1.1rem;border-bottom:1px solid rgba(120,191,224,.14)}.hl-account-head h2{margin:.2rem 0 0}.hl-account-head button{border:0;background:transparent;color:#fff;font-size:1.6rem;cursor:pointer}.hl-account-body{padding:1rem;display:grid;gap:.8rem;overflow:auto;max-height:66vh}.hl-account-card{display:grid;gap:.55rem;padding:1rem;border:1px solid rgba(120,191,224,.14);border-radius:18px;background:rgba(255,255,255,.035)}.hl-account-card.overdue{border-color:rgba(255,128,105,.45);background:rgba(255,92,70,.055)}.hl-account-card-head{display:flex;align-items:flex-start;justify-content:space-between;gap:.8rem}.hl-account-card h3{margin:0;font-size:1rem}.hl-account-card p,.hl-account-card small{margin:0;color:#9fbdcc}.hl-status{font-size:.72rem;font-weight:900;padding:.3rem .55rem;border-radius:999px;background:rgba(255,255,255,.08)}.hl-status.confirmed,.hl-status.active,.hl-status.returned,.hl-status.read{color:#90e7ba;background:rgba(68,190,126,.12)}.hl-status.cancelled,.hl-status.rejected,.hl-status.overdue{color:#ffaaa0;background:rgba(255,100,90,.11)}.hl-status.pending,.hl-status.reserved{color:#f6d88d;background:rgba(229,180,95,.12)}.hl-account-actions{display:flex;gap:.55rem;flex-wrap:wrap}.hl-account-actions button{border:1px solid rgba(120,191,224,.18);border-radius:12px;padding:.58rem .8rem;background:rgba(255,255,255,.05);color:#eaf7fb;font-weight:800;cursor:pointer}.hl-account-actions button.primary{background:linear-gradient(135deg,#f4d18c,#e5b45f);color:#102131;border:0}.hl-account-actions button.danger{border-color:rgba(255,120,110,.28);color:#ffc0b9}.hl-account-actions button:disabled{opacity:.5;cursor:not-allowed}.hl-account-empty{text-align:center;padding:2rem 1rem;color:#9fbdcc}.hl-account-error{color:#ffc0b9;text-align:center;padding:1rem}.hl-notification.unread{border-color:rgba(229,180,95,.32);background:rgba(229,180,95,.05)}.hl-rental-items{display:grid;gap:.45rem}.hl-rental-item{display:flex;justify-content:space-between;gap:.7rem;padding:.65rem;border-radius:12px;background:rgba(255,255,255,.04)}.hl-rental-item b{display:block}.hl-rental-item span{text-align:left;color:#bcd3de;font-size:.8rem}.hl-overdue-note{padding:.7rem;border-radius:12px;background:rgba(255,100,90,.09);color:#ffc0b9;font-weight:800}@media(max-width:640px){.hl-account-dialog{width:calc(100vw - 16px);border-radius:20px}.hl-account-card-head{align-items:flex-start;flex-direction:column}.hl-rental-item{flex-direction:column}.hl-rental-item span{text-align:right}}`;document.head.appendChild(style);};
  const ensureDialog=(id,title,eyebrow)=>{let dialog=document.getElementById(id);if(dialog)return dialog;dialog=document.createElement('dialog');dialog.id=id;dialog.className='hl-account-dialog';dialog.innerHTML=`<div class="hl-account-head"><div><p class="eyebrow">${esc(eyebrow)}</p><h2>${esc(title)}</h2></div><button type="button" aria-label="إغلاق">×</button></div><div class="hl-account-body"></div>`;dialog.querySelector('.hl-account-head button').addEventListener('click',()=>dialog.close());dialog.addEventListener('click',event=>{if(event.target===dialog)dialog.close()});document.body.appendChild(dialog);return dialog;};
  const requireAuth=()=>{if(auth()?.isAuthenticated?.())return true;toast('سجّل الدخول أولًا');return false};
  const formatDate=value=>{if(!value)return '—';const date=new Date(value);return Number.isNaN(date.getTime())?'—':date.toLocaleString('ar-SA')};
  const renderBookings=async()=>{if(!requireAuth())return;ensureStyle();const dialog=ensureDialog('hl-bookings-dialog','حجوزاتي','MY BOOKINGS · حجوزاتي');const body=dialog.querySelector('.hl-account-body');body.innerHTML='<div class="hl-account-empty">جارٍ تحميل الحجوزات...</div>';dialog.showModal();try{const rows=await request('/trips/bookings/mine',{method:'GET'});const bookings=Array.isArray(rows)?rows:[];if(!bookings.length){body.innerHTML='<div class="hl-account-empty">لا توجد حجوزات حتى الآن.</div>';return}body.innerHTML=bookings.map(item=>{const status=String(item.status||'PENDING').toLowerCase();const cancellable=['pending','confirmed'].includes(status)&&item.trip&&new Date(item.trip.startsAt)>new Date();return `<article class="hl-account-card" data-booking-id="${esc(item.id)}"><div class="hl-account-card-head"><div><h3>${esc(item.trip?.title||'رحلة')}</h3><p>${esc(formatDate(item.trip?.startsAt))}</p></div><span class="hl-status ${esc(status)}">${esc(item.status||'PENDING')}</span></div><small>عدد المقاعد: ${esc(item.seats||1)}</small><div class="hl-account-actions">${cancellable?'<button class="danger" data-cancel-booking>إلغاء الحجز</button>':''}<button data-booking-participants>المشاركون</button></div></article>`}).join('');body.querySelectorAll('[data-cancel-booking]').forEach(button=>button.addEventListener('click',async()=>{const bookingId=button.closest('[data-booking-id]')?.dataset.bookingId;if(!bookingId)return;button.disabled=true;try{await request(`/trips/bookings/${encodeURIComponent(bookingId)}/cancel`,{method:'PATCH'});toast('تم إلغاء الحجز');dialog.close();await renderBookings()}catch(error){toast(error instanceof Error?error.message:'تعذر إلغاء الحجز');button.disabled=false}}));body.querySelectorAll('[data-booking-participants]').forEach(button=>button.addEventListener('click',async()=>{const bookingId=button.closest('[data-booking-id]')?.dataset.bookingId;if(!bookingId)return;button.disabled=true;try{const participants=await request(`/trips/bookings/${encodeURIComponent(bookingId)}/participants`,{method:'GET'});const names=(Array.isArray(participants)?participants:[]).map((p,index)=>p.fullName||`مشارك ${index+1}`).join('، ');toast(names||'لا توجد بيانات مشاركين')}catch(error){toast(error instanceof Error?error.message:'تعذر تحميل المشاركين')}finally{button.disabled=false}}))}catch(error){body.innerHTML=`<div class="hl-account-error">${esc(error instanceof Error?error.message:'تعذر تحميل الحجوزات')}</div>`}};
  const renderRentals=async()=>{if(!requireAuth())return;ensureStyle();const dialog=ensureDialog('hl-rentals-dialog','إيجاراتي','MY RENTALS · إيجاراتي');const body=dialog.querySelector('.hl-account-body');body.innerHTML='<div class="hl-account-empty">جارٍ تحميل إيجارات المعدات...</div>';dialog.showModal();try{const rows=await request('/trips/equipment-rentals/mine');const rentals=Array.isArray(rows)?rows:[];if(!rentals.length){body.innerHTML='<div class="hl-account-empty">لا توجد إيجارات معدات حتى الآن.</div>';return}body.innerHTML=rentals.map(rental=>{const status=String(rental.status||'RESERVED').toLowerCase(),items=(rental.items||[]).map(item=>`<div class="hl-rental-item"><div><b>${esc(item.equipmentNameSnapshot)}</b><small>${esc(item.assetCodeSnapshot)}${item.serialNumberSnapshot?` · S/N ${esc(item.serialNumberSnapshot)}`:''}</small></div><span>${esc(item.equipmentType||'معدات غوص')} · المقاس ${esc(item.size||'—')}<br>${sar(item.unitPriceHalala)} · ${item.returnedAt?'تم الإرجاع':'مع الغواص'}</span></div>`).join(''),extension=rental.extensionStatus==='PENDING'?'<small>طلب التمديد: قيد المراجعة</small>':rental.extensionStatus==='APPROVED'?'<small>تمت الموافقة على آخر طلب تمديد.</small>':rental.extensionStatus==='REJECTED'?'<small>تم رفض آخر طلب تمديد.</small>':'';return `<article class="hl-account-card ${rental.overdue?'overdue':''}" data-rental-id="${esc(rental.id)}"><div class="hl-account-card-head"><div><h3>فاتورة ${esc(rental.invoiceNumber)}</h3><p>${formatDate(rental.reservedAt)} · ${rental.paymentStatus==='PAID'?'مدفوعة':'بانتظار الدفع'}</p></div><span class="hl-status ${rental.overdue?'overdue':esc(status)}">${rental.overdue?'متأخر':esc(rental.status)}</span></div>${rental.dueAt?`<small>موعد الإرجاع: <b>${esc(formatDate(rental.dueAt))}</b></small>`:''}${rental.overdue?'<div class="hl-overdue-note">انتهت مدة الإيجار. مدّد الإيجار أو أعد المعدات للمستودع.</div>':''}<div class="hl-rental-items">${items}</div><small>الإجمالي: <b>${sar(rental.totalHalala)}</b> · المتبقي للإرجاع: ${esc(rental.pendingReturnCount||0)}</small>${extension}${rental.returnedAt?`<small>تاريخ إغلاق الإيجار: ${esc(formatDate(rental.returnedAt))}</small>`:''}${rental.status==='ACTIVE'?`<div class="hl-account-actions"><button class="primary" data-rental-extend ${rental.extensionStatus==='PENDING'?'disabled':''}>تمديد الإيجار</button><button data-rental-return>سأعيد المعدات</button></div>`:''}</article>`}).join('');body.querySelectorAll('[data-rental-extend]').forEach(button=>button.addEventListener('click',async()=>{const rentalId=button.closest('[data-rental-id]')?.dataset.rentalId;if(!rentalId)return;const value=prompt('أدخل تاريخ ووقت التمديد الجديد بصيغة YYYY-MM-DD HH:MM');if(!value)return;const parsed=new Date(value.replace(' ','T'));if(Number.isNaN(parsed.getTime())){toast('صيغة التاريخ غير صحيحة');return}button.disabled=true;try{await request(`/trips/equipment-rentals/${encodeURIComponent(rentalId)}/extension-request`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({requestedUntil:parsed.toISOString()})});toast('تم إرسال طلب التمديد للمراجعة');dialog.close();await renderRentals()}catch(error){toast(error instanceof Error?error.message:'تعذر إرسال طلب التمديد');button.disabled=false}}));body.querySelectorAll('[data-rental-return]').forEach(button=>button.addEventListener('click',async()=>{const rentalId=button.closest('[data-rental-id]')?.dataset.rentalId;if(!rentalId)return;button.disabled=true;try{await request(`/trips/equipment-rentals/${encodeURIComponent(rentalId)}/return-intent`,{method:'POST'});toast('تم إشعار المستودع بأنك ستعيد المعدات');button.textContent='تم إشعار المستودع'}catch(error){toast(error instanceof Error?error.message:'تعذر إرسال إشعار الإرجاع');button.disabled=false}}))}catch(error){body.innerHTML=`<div class="hl-account-error">${esc(error instanceof Error?error.message:'تعذر تحميل الإيجارات')}</div>`}};
  const notificationSummary=payload=>{if(!payload||typeof payload!=='object')return '';const entries=Object.entries(payload).filter(([,value])=>['string','number','boolean'].includes(typeof value)).slice(0,4);return entries.map(([key,value])=>`${key}: ${value}`).join(' · ')};
  const renderNotifications=async()=>{if(!requireAuth())return;ensureStyle();const dialog=ensureDialog('hl-notifications-dialog','الإشعارات','NOTIFICATIONS · الإشعارات');const body=dialog.querySelector('.hl-account-body');body.innerHTML='<div class="hl-account-empty">جارٍ تحميل الإشعارات...</div>';dialog.showModal();try{const rows=await request('/notifications',{method:'GET'});const notifications=Array.isArray(rows)?rows:[];if(!notifications.length){body.innerHTML='<div class="hl-account-empty">لا توجد إشعارات جديدة.</div>';return}body.innerHTML=notifications.map(item=>{const unread=String(item.status||'UNREAD')!=='READ';return `<article class="hl-account-card hl-notification ${unread?'unread':''}" data-notification-id="${esc(item.id)}"><div class="hl-account-card-head"><div><h3>${esc(item.type||'إشعار')}</h3><p>${esc(formatDate(item.createdAt))}</p></div><span class="hl-status ${unread?'pending':'read'}">${unread?'جديد':'مقروء'}</span></div><small>${esc(notificationSummary(item.payload))}</small>${unread?'<div class="hl-account-actions"><button data-mark-read>تحديد كمقروء</button></div>':''}</article>`}).join('');body.querySelectorAll('[data-mark-read]').forEach(button=>button.addEventListener('click',async()=>{const id=button.closest('[data-notification-id]')?.dataset.notificationId;if(!id)return;button.disabled=true;try{await request(`/notifications/${encodeURIComponent(id)}/read`,{method:'POST'});dialog.close();await renderNotifications()}catch(error){toast(error instanceof Error?error.message:'تعذر تحديث الإشعار');button.disabled=false}}))}catch(error){body.innerHTML=`<div class="hl-account-error">${esc(error instanceof Error?error.message:'تعذر تحميل الإشعارات')}</div>`}};
  const bind=()=>{const nextTrip=document.querySelector('.mobile-welcome button');if(nextTrip&&!nextTrip.dataset.hlBound){nextTrip.dataset.hlBound='1';nextTrip.removeAttribute('data-toast');nextTrip.addEventListener('click',event=>{event.preventDefault();event.stopImmediatePropagation();renderBookings()},true)}document.querySelectorAll('.profile-list button').forEach(button=>{const label=button.textContent||'';if(label.includes('الإشعارات')&&!button.dataset.hlBound){button.dataset.hlBound='1';button.removeAttribute('data-toast');button.addEventListener('click',event=>{event.preventDefault();event.stopImmediatePropagation();document.getElementById('profile-dialog')?.close();renderNotifications()},true)}});const topActions=document.querySelectorAll('.top-actions .icon-button');const notificationsButton=topActions[1];if(notificationsButton&&!notificationsButton.dataset.hlBound){notificationsButton.dataset.hlBound='1';notificationsButton.setAttribute('aria-label','فتح الإشعارات');notificationsButton.addEventListener('click',event=>{event.preventDefault();renderNotifications()})}const profileList=document.querySelector('.profile-list');if(profileList&&!profileList.querySelector('[data-hl-bookings]')){const bookings=document.createElement('button');bookings.dataset.hlBookings='1';bookings.innerHTML='<span>⌖</span>حجوزاتي <b>←</b>';bookings.addEventListener('click',()=>{document.getElementById('profile-dialog')?.close();renderBookings()});profileList.prepend(bookings)}if(profileList&&!profileList.querySelector('[data-hl-rentals]')){const rentals=document.createElement('button');rentals.dataset.hlRentals='1';rentals.innerHTML='<span>▣</span>إيجاراتي <b>←</b>';rentals.addEventListener('click',()=>{document.getElementById('profile-dialog')?.close();renderRentals()});profileList.prepend(rentals)}};
  document.addEventListener('hydroland:auth-changed',bind);bind();window.HydrolandAccountCenter={openBookings:renderBookings,openRentals:renderRentals,openNotifications:renderNotifications};
})();