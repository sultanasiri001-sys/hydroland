(()=>{const host=document.getElementById('role-console')||document.querySelector('main');if(!host)return;const panel=document.createElement('section');panel.className='hl-weather-admin';panel.hidden=true;panel.innerHTML='<div class="hl-admin__header"><div><span class="eyebrow">WEATHER GATE</span><h3>بوابة الطقس والتقويم</h3><p>تحكم مركزي في تأثير حالة الطقس والبحر على الحجوزات.</p></div><span data-weather-state>بانتظار الاتصال</span></div><div class="hl-admin__grid"><article class="hl-admin__panel"><div class="hl-admin__panel-title"><h3>حالة البوابة</h3><strong data-weather-enabled>—</strong></div><button class="btn btn-primary" type="button" data-weather-toggle>تفعيل</button></article><article class="hl-admin__panel"><div class="hl-admin__panel-title"><h3>وضع التشغيل</h3><strong data-weather-mode>—</strong></div><button class="btn" type="button" data-weather-advisory>عرض فقط</button><button class="btn" type="button" data-weather-enforce>تطبيق على الحجز</button></article><article class="hl-admin__panel"><div class="hl-admin__panel-title"><h3>مزود البيانات</h3><strong data-weather-provider>—</strong></div><p>لن يتم تفعيل أي مزود خارجي حتى يتم اعتماده وربطه رسميًا.</p></article></div>';host.insertAdjacentElement('afterend',panel);const api=()=>window.HydrolandAuth?.apiBase||window.HYDROLAND_API_BASE||'https://hydroland.onrender.com/api/v1',token=()=>window.HydrolandAuth?.getAccessToken?.(),headers=()=>({'Content-Type':'application/json',Authorization:'Bearer '+token()}),state=t=>{const e=panel.querySelector('[data-weather-state]');if(e)e.textContent=t};function render(settings){const enabled=panel.querySelector('[data-weather-enabled]');if(enabled)enabled.textContent=settings.enabled?'مفعلة':'متوقفة';const mode=panel.querySelector('[data-weather-mode]');if(mode)mode.textContent=settings.mode==='ENFORCE'?'تطبيق على الحجز':'عرض فقط';const provider=panel.querySelector('[data-weather-provider]');if(provider)provider.textContent=settings.provider==='NOT_SELECTED'?'غير محدد':settings.provider||'غير محدد';const toggle=panel.querySelector('[data-weather-toggle]');if(toggle)toggle.textContent=settings.enabled?'إيقاف':'تفعيل';panel.dataset.enabled=String(Boolean(settings.enabled))}async function load(){if(!token()){state('يتطلب دخول إداري');return}try{state('جارٍ تحميل الإعدادات');const r=await fetch(api()+'/trips/admin/weather-gate',{headers:headers()}),body=await r.json();if(!r.ok)throw new Error();render(body);state('متصل بالخادم')}catch{state('تعذر تحميل الإعدادات')}}async function save(payload){try{state('جارٍ حفظ الإعداد');const r=await fetch(api()+'/trips/admin/weather-gate',{method:'PATCH',headers:headers(),body:JSON.stringify(payload)}),body=await r.json();if(!r.ok)throw new Error();render(body);window.dispatchEvent(new CustomEvent('hydroland:weather-gate-changed',{detail:body}));state('تم حفظ الإعداد')}catch{state('تعذر حفظ الإعداد')}}panel.querySelector('[data-weather-toggle]')?.addEventListener('click',()=>save({enabled:panel.dataset.enabled!=='true'}));panel.querySelector('[data-weather-advisory]')?.addEventListener('click',()=>save({mode:'ADVISORY'}));panel.querySelector('[data-weather-enforce]')?.addEventListener('click',()=>save({mode:'ENFORCE'}));document.addEventListener('hydroland:role-changed',e=>{const active=e.detail?.role==='admin';panel.hidden=!active;if(active)load()});document.addEventListener('hydroland:auth-changed',()=>{if(!panel.hidden)load()});})();