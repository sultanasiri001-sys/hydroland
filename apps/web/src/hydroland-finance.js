(()=>{const anchor=document.getElementById('role-console')||document.querySelector('.membership')||document.querySelector('main');if(!anchor||document.querySelector('.hl-finance'))return;const auth=()=>window.HydrolandAuth;const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const sar=v=>new Intl.NumberFormat('ar-SA',{style:'currency',currency:'SAR'}).format(Number(v||0)/100);const section=document.createElement('section');section.className='hl-finance';section.innerHTML=`<header class="hl-finance-head"><div><small>HYDROLAND FINANCE</small><h3>المدفوعات والاستردادات</h3></div><span data-finance-state>يتطلب تسجيل الدخول</span></header><div class="hl-finance-kpis"><article class="hl-finance-kpi"><small>عملياتي</small><b data-finance-count>—</b><span>من الخادم</span></article><article class="hl-finance-kpi"><small>إجمالي القيمة المسجلة</small><b data-finance-total>—</b><span>حسب بيانات الحساب</span></article></div><div class="hl-finance-grid"><section class="hl-finance-panel"><h4>سجل مدفوعاتي</h4><div data-finance-list><p>سجّل الدخول لعرض المدفوعات الفعلية.</p></div><div class="hl-finance-actions"><button data-finance-refresh>تحديث</button></div></section><section class="hl-finance-panel"><h4>حدود الوحدة الحالية</h4><p>الخادم يدعم إنشاء الدفع، عرض مدفوعات الحساب، وطلب الاسترداد. لا توجد حاليًا API موثقة للتسويات أو عمولات الشركاء أو التقارير المالية المجمعة؛ لذلك لا تعرض المنصة أرقامًا تقديرية لهذه البنود.</p></section></div>`;anchor.insertAdjacentElement('afterend',section);const state=section.querySelector('[data-finance-state]');const list=section.querySelector('[data-finance-list]');async function load(){if(!auth()?.isAuthenticated?.()){state.textContent='يتطلب تسجيل الدخول';list.innerHTML='<p>سجّل الدخول لعرض المدفوعات الفعلية.</p>';return}const client=auth()?.authorizedFetch;if(!client)return;state.textContent='جارٍ التحديث';try{const r=await client('/payments/mine');if(!r.ok)throw new Error(`HTTP_${r.status}`);const rows=await r.json(),items=Array.isArray(rows)?rows:[];section.querySelector('[data-finance-count]').textContent=String(items.length);section.querySelector('[data-finance-total]').textContent=sar(items.reduce((sum,x)=>sum+Number(x.amountMinor||0),0));list.innerHTML=items.length?items.map(x=>`<div class="hl-finance-row"><strong>${esc(x.id||'عملية دفع')}</strong><small>${esc(x.bookingId||'')}</small><span>${sar(x.amountMinor)}</span><em class="hl-finance-status">${esc(x.status||'—')}</em></div>`).join(''):'<p>لا توجد مدفوعات مسجلة لهذا الحساب.</p>';state.textContent='متصل بالخادم'}catch{state.textContent='تعذر تحميل المدفوعات';list.innerHTML='<p>تعذر الوصول إلى بيانات المدفوعات.</p>'}}section.querySelector('[data-finance-refresh]').addEventListener('click',load);document.addEventListener('hydroland:auth-changed',load);setTimeout(load,600);})();