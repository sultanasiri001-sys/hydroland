(()=>{
  const auth=()=>window.HydrolandAuth;
  const toast=message=>{const target=document.getElementById('toast');if(!target)return;target.textContent=message;target.classList.add('visible');setTimeout(()=>target.classList.remove('visible'),2400)};
  const request=async(path,options={})=>{const client=auth()?.authorizedFetch;if(!client)throw new Error('سجّل الدخول أولًا');const response=await client(path,options);const body=await response.json().catch(()=>null);if(!response.ok)throw new Error(body?.message||`تعذر تنفيذ الطلب (${response.status})`);return body};
  const ensureDialog=()=>{let dialog=document.getElementById('hl-credential-create');if(dialog)return dialog;dialog=document.createElement('dialog');dialog.id='hl-credential-create';dialog.className='hl-credential-create';dialog.innerHTML=`<form><header><div><small>CREDENTIAL · بيانات الاعتماد</small><h2>إضافة بيانات شهادة</h2></div><button type="button" data-close aria-label="إغلاق">×</button></header><div class="hl-credential-grid"><label>اسم الشهادة<input name="title" required maxlength="160"></label><label>الجهة المصدرة<input name="issuer" required maxlength="160"></label><label>رقم الشهادة<input name="credentialNumber" maxlength="120"></label><label>تاريخ الإصدار<input name="issuedAt" type="date"></label><label>تاريخ الانتهاء<input name="expiresAt" type="date"></label></div><p class="hl-credential-note">هذه الخطوة تحفظ بيانات الشهادة فقط. رفع المستند وطلب التحقق غير متاحين حتى ربط خدمة التخزين.</p><p class="hl-credential-status" role="status" aria-live="polite"></p><footer><button type="button" data-cancel>إلغاء</button><button type="submit" class="primary-button">حفظ البيانات</button></footer></form>`;const style=document.createElement('style');style.textContent=`.hl-credential-create{width:min(660px,calc(100vw - 24px));border:1px solid rgba(120,191,224,.22);border-radius:24px;background:#071f31;color:#edf7fb;padding:0;box-shadow:0 28px 90px rgba(0,0,0,.48)}.hl-credential-create::backdrop{background:rgba(1,10,18,.72);backdrop-filter:blur(5px)}.hl-credential-create form{display:grid;gap:1rem;padding:1.1rem}.hl-credential-create header,.hl-credential-create footer{display:flex;align-items:center;justify-content:space-between;gap:.8rem}.hl-credential-create header h2{margin:.2rem 0 0}.hl-credential-create header button{border:0;background:transparent;color:#fff;font-size:1.6rem}.hl-credential-grid{display:grid;grid-template-columns:1fr 1fr;gap:.8rem}.hl-credential-grid label{display:grid;gap:.35rem;font-weight:800}.hl-credential-grid input{width:100%;box-sizing:border-box;border:1px solid rgba(120,191,224,.24);border-radius:12px;background:rgba(255,255,255,.055);color:#fff;padding:.78rem;font:inherit}.hl-credential-note{margin:0;color:#b7ced9;line-height:1.6}.hl-credential-status{min-height:1.4rem;margin:0;color:#ffc0b9}.hl-credential-create footer{justify-content:flex-end}.hl-credential-create footer button{border:1px solid rgba(120,191,224,.2);border-radius:12px;padding:.7rem 1rem;background:transparent;color:#eaf7fb;font-weight:800}.hl-credential-create footer .primary-button{border:0;color:#102131}@media(max-width:600px){.hl-credential-grid{grid-template-columns:1fr}}`;document.head.appendChild(style);const form=dialog.querySelector('form'),close=()=>dialog.close();dialog.querySelector('[data-close]').addEventListener('click',close);dialog.querySelector('[data-cancel]').addEventListener('click',close);dialog.addEventListener('click',event=>{if(event.target===dialog)close()});form.addEventListener('submit',async event=>{event.preventDefault();const data=new FormData(form),submit=form.querySelector('[type="submit"]'),status=form.querySelector('.hl-credential-status'),title=String(data.get('title')||'').trim(),issuer=String(data.get('issuer')||'').trim();if(!title||!issuer){status.textContent='اسم الشهادة والجهة المصدرة مطلوبان';return}const payload={title,issuer},credentialNumber=String(data.get('credentialNumber')||'').trim(),issuedAt=String(data.get('issuedAt')||''),expiresAt=String(data.get('expiresAt')||'');if(credentialNumber)payload.credentialNumber=credentialNumber;if(issuedAt)payload.issuedAt=issuedAt;if(expiresAt)payload.expiresAt=expiresAt;submit.disabled=true;status.textContent='جارٍ حفظ بيانات الشهادة...';try{await request('/credentials',{method:'POST',body:JSON.stringify(payload)});form.reset();close();toast('تم حفظ بيانات الشهادة');await window.HydrolandProfile?.load?.()}catch(error){status.textContent=error instanceof Error?error.message:'تعذر حفظ بيانات الشهادة'}finally{submit.disabled=false}});document.body.appendChild(dialog);return dialog};
  document.addEventListener('click',event=>{const button=event.target.closest?.('[data-hl-action="credential-create"]');if(!button)return;event.preventDefault();if(!auth()?.isAuthenticated?.()){toast('سجّل الدخول أولًا لإضافة شهادة');return}ensureDialog().showModal()});
})();
